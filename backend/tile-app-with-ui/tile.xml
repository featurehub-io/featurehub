<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <properties>
    <docker.prefix>featurehub</docker.prefix>
    <docker.project-name>${project.artifactId}</docker.project-name>
  </properties>
  <profiles>
    <profile>
      <id>docker-cloud-build</id>
      <!-- otherwise don't active in tiles -->
      <activation>
        <property>
          <name>docker-cloud-build</name>
          <value>true</value>
        </property>
      </activation>
      <dependencies>
        <!-- this is only here to ensure build order for the docker image -->
        <dependency>
          <groupId>io.featurehub.admin_ui</groupId>
          <artifactId>admin-frontend</artifactId>
          <version>1.1-SNAPSHOT</version>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>pl.project13.maven</groupId>
            <artifactId>git-commit-id-plugin</artifactId>
            <version>4.0.0</version>
            <executions>
              <execution>
                <id>get-info</id>
                <phase>initialize</phase>
                <goals>
                  <goal>revision</goal>
                </goals>
            </execution>
            </executions>
            <configuration>
              <generateGitPropertiesFile>false</generateGitPropertiesFile>
              <verbose>true</verbose>
              <dateFormat>yyyy-MM-dd'T'HH:mm:ss'Z'</dateFormat>
              <includeOnlyProperties>
                <includeOnlyProperty>git.commit.time</includeOnlyProperty>
              </includeOnlyProperties>
            </configuration>
          </plugin>
          <plugin>
            <groupId>cd.connect.pipeline</groupId>
            <artifactId>docker-manifest-collectosaur-plugin</artifactId>
            <version>1.2</version>
            <executions>
              <execution>
                <id>mamasaur</id>
                <phase>compile</phase>
                <goals>
                  <goal>babysaur</goal>
                </goals>
                <configuration>
                  <fullImageName>featurehub/${project.artifactId}:${build.version}</fullImageName>
                  <baseImageName>featurehub/${project.artifactId}:${build.version}</baseImageName>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <!-- grab the latest admin-ui and unzip into  the target folder -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack admin-ui</id>
                <phase>initialize</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>io.featurehub.admin_ui</groupId>
                      <artifactId>admin-frontend</artifactId>
                      <version>1.1-SNAPSHOT</version>
                      <type>zip</type>
                      <outputDirectory>${project.basedir}/target/admin-ui</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                  <overWriteReleases>true</overWriteReleases>
                  <overWriteSnapshots>true</overWriteSnapshots>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.google.cloud.tools</groupId>
            <artifactId>jib-maven-plugin</artifactId>
            <version>2.3.0</version>
            <executions>
              <execution>
                <phase>package</phase>
                <goals>
                  <goal>dockerBuild</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <allowInsecureRegistries>false</allowInsecureRegistries>
              <from>
                <image>${app.baseimage}</image>
              </from>
              <to>
                <image>${docker.prefix}/${docker.project-name}:${build.version}</image>
              </to>
              <extraDirectories>
                <paths>
                  <path>
                    <from>${project.basedir}/target/admin-ui</from>
                    <into>/var/www/html</into>
                  </path>
                </paths>
              </extraDirectories>
              <container>
                <mainClass>bathe.BatheBooter</mainClass>
                <jvmFlags>
                  <jvmFlag>--add-exports=java.base/jdk.internal.misc=ALL-UNNAMED</jvmFlag>
                  <jvmFlag>--add-opens=java.base/java.nio=ALL-UNNAMED</jvmFlag>
                  <jvmFlag>-Dlog4j.configurationFile=/etc/common-config/log4j2.xml</jvmFlag>
                  <jvmFlag>-XX:+UseContainerSupport</jvmFlag>
                  <jvmFlag>-XX:MaxRAMPercentage=80</jvmFlag>
                </jvmFlags>
                <filesModificationTime>${git.commit.time}</filesModificationTime>
                <args>
                  <arg>-R${app.entrypoint}</arg>
                  <arg>-P/etc/common-config/common.properties</arg>
                  <arg>-P/etc/app-config/application.properties</arg>
                </args>
                <ports>
                  <port>${app.port}</port>
                </ports>
              </container>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>


</project>
