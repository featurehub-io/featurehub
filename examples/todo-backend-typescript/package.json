{
  "name": "typescript-todos-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "link": "npm link featurehub-repository && npm link featurehub-eventsource",
    "build": "node_modules/.bin/tsc",
    "compile": "npm run link && npm run build && cp package.json dist && cd dist",
    "start": "npm run link && npm run build && node dist/app.js"
  },
  "dependencies": {
    "eventsource": "^1.0.7",
    "restify": "^8.5.1",
    "restify-cors-middleware": "^1.1.1"
  },
  "devDependencies": {
    "@types/restify": "^8.4.2",
    "@types/restify-cors-middleware": "^1.0.1",
    "@types/node": "^12.12.0",
    "typescript": "^3.9.7"
  }
}
